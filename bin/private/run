#!/bin/bash

###
# run given command with echo
# if $DRY_RUN is set, no execution
# @function run
###
run () {
    echo -e executing command: $*

    if [[ ! -n "$DRY_RUN" ]]; then
        $*
    fi
}
