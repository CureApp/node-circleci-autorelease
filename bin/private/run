#!/bin/bash

###
# run given command with echo
# if $DRY_RUN is set, no execution
# @function run
###
run () {
    echo -e "executing command: \033[0;32m$@\033[0m" >&2

    if [[ ! -n "$DRY_RUN" ]]; then
        "$@"
    fi
}
